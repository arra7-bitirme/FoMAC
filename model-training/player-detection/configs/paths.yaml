# Path Configuration
# All paths can be absolute or relative to workspace root

workspace_root: "."
snmot_root: "tracking"
output_root: "datasets/snmot_detection"
models_root: "models"
reports_root: "reports"  # Root directory for all training reports

# Dataset structure
dataset_yaml: "dataset.yaml"
train_split: "train"
val_split: "test"
test_split: "test"

# Legacy SoccerNet settings (kept for backward compatibility)
soccernet_root: "soccerNet"
video_pattern: "{half}_720p.mkv"
annotation_pattern: "{half}_player_boundingbox_maskrcnn.json"