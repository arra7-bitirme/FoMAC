# Visualization configuration

tensorboard:
  enabled: false          # Set true to launch TensorBoard automatically
  logdir: "models"       # Directory containing YOLO training runs
  host: "127.0.0.1"
  port: 6006
  open_browser: false     # Auto-open default browser to the dashboard
  keep_alive: false       # Keep server alive after pipeline exits
