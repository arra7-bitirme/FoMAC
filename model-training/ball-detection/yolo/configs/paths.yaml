# Path Configuration
# All paths can be absolute or relative to workspace root

workspace_root: "/home/alperen/fomac/FoMAC/model-training/ball-detection"
ballDataset_root: "ballDataset"
soccernet_root: "soccerNet"  # Keep for compatibility
output_root: "ballDataset"  # Use the existing prepared dataset
models_root: "models"
reports_root: "reports"  # Root directory for all training reports

# Dataset structure - use existing ballDataset format
dataset_yaml: "ballDataset/data.yaml"
train_split: "train"
val_split: "val"
test_split: "test"

# Video and annotation file patterns (for future extensions)
video_pattern: "{half}_720p.mkv"
annotation_pattern: "{half}_ball_boundingbox.json"